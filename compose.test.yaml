version: '3'
services:
  redis:
    image: redis:alpine
    tty: true

  app-ubuntu:
    build:
      context: .
      dockerfile: ./docker/nim/ubuntu/Dockerfile
      args:
        VERSION: 2.0.0
    tty: true
    volumes:
      - .:/root/project
